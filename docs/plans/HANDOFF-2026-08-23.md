# Handoff — Calendar-master, 23 Aug 2026

Read this first, then `docs/plans/HANDOFF.md` (19 Aug). **That document supersedes
nothing here and is superseded by nothing here**: its State section and test
baselines are stale, but its environment traps and working agreements are still
the reference and are not repeated in full below.

---

## State

- `main` is at **`af5de2d`** (PR #12 merged).
- **PR #13 is open** on `claude/repo-crawl-overview-g3on95` (`3bdee6c`) — four
  design-debt fixes plus a plan status correction. Not merged.
- `Planner.jsx` is **5,545 lines**, ratchet ceiling **5,545** — again at zero
  headroom. The next line added anywhere in it fails `npm test`. Extract first.
- 95 unit test files, 35 browser specs, 60 modules under `src/features/`.

### Test baseline — measure it yourself, never inherit it

Measured on this machine, this day, nothing else running:

| | passed | failed | wall clock |
| --- | --- | --- | --- |
| `main` at `af5de2d` | 363 | 2 | 12.1m |
| PR #13 head | **369** | **2** | 12.2m |
| `npm test` | 645 | 0 | ~10s |

**The two failures are the same two in both runs and are not anyone's current
work:**

| Spec | Why it is red |
| --- | --- |
| `motion.spec.js:503` "a stalled Composer body is repaired…" | Asserts a repair that is **not implemented**. It pauses `nbnotchbodyin` at 10% and expects it gone once the stage opens, but `Sheet.jsx`'s stage machine is pure `setTimeout` — it flips `data-morph-stage` and cancels nothing. A paused CSS animation never leaves `getAnimations()`, so this cannot pass as written. **Has never passed** — fails at `65072e4`, the commit that added it. |
| `interaction-feedback.spec.js:41` "focus remains visible…" | Asserts a focus ring after a bare `.focus()`, which Chromium correctly does not treat as `:focus-visible`. The control **does** get its 2px ring under real keyboard focus (verified against `search-control`, `hud-notes`, `nav-toggle`, `actions-add`). A fragile test, not a product defect. |

`motion.spec.js:727` was also red for most of this session and now passes — it is
a **race**, not a defect: it reads the source label after two `toHaveAttribute`
polls, by which point `nbnotchlabelout` has started. Its failing values
(`-11.32px`, `-19.27px`) are points on that curve, not a leaked transform.

**The pass/fail set is browser-version dependent.** Codex reports these three
green on Chromium 151; they are red on this box's Chromium 1194. Neither party is
wrong. Do not treat any focused-suite number from another machine as a baseline
you can reproduce.

---

## The one thing that actually matters and is still open

**Nobody has measured the navigation stutter on a phone.**

PR #11 rebuilt nav travel to stop animating `clip-path` per frame, and PR #12
fixed the corner regression that shipped with it. Neither closes the original
user report, because the defect is paint-bound and **headless Chromium cannot see
it** — it has no display pipeline and charges almost nothing for paint. Measured
here: a clean 60fps, median 16.7ms, worst 16.8ms, zero frames over 33ms, zero
Long-Animation-Frame entries. The same recorder caught the sheet morph at worst
50ms in the same session, so the instrument works.

`docs/plans/2026-08-23-001-fix-navigation-shell-clip-path-repaint-plan.md` §3 has
the gate. In short: remote-debug the phone, Rendering → **Paint flashing**, open
and close the nav. If the whole app surface flashes through the travel, it is not
fixed. A Performance capture showing Paint bars spanning the 520ms says the same.

Until someone does that, the honest status is *implementation complete, defect
unconfirmed*.

---

## What the user reported that could NOT be reproduced

Five regressions were reported on 23 Aug. One was real; four were not
reproducible on either `e5c243e` or PR #11's head, at 1280×900, with the sample
notebook:

| Report | Finding |
| --- | --- |
| Four inverted nav corners | **Real.** Root-caused, fixed in PR #12. |
| Event/Action drag and resize broken | Not reproduced. Event body drag works 3/3 consecutive; edge resize works; action drag works — it detaches the recurring occurrence and re-plans it, which is documented behaviour. |
| Random line across the app | Not reproduced as a defect. Codex identified it as the Day current-time marker; the only stray line found here was `[data-test="task-add-step"]`'s intentional `border-left: 2px` checklist rail. |
| ANY TIME missing | Not reproduced. Present in Day, correctly absent in Week. |
| Timeline edge scroll stutter | Not reproduced — **but the instrument is weak**. `timelineInteractionState.js` says in its own comment that synthetic wheel input does not move the stream, so an e2e harness cannot drive that state. Treat as *untested*, not cleared. |

**If any of these recur, first establish which build is on screen.** The repo
documents this exact failure mode in `.planning/codebase/CONCERNS.md` — a stale
Vite listener on 5000/4323 serving a different checkout. Paste into the console of
the tab showing the bug:

```js
({ masks: document.querySelectorAll('[data-nav-mask]').length,   // 8 = PR#11+, 0 = older
   anyTime: !!document.evaluate("//*[text()='ANY TIME']",document,null,9,null).singleNodeValue,
   movers: document.querySelectorAll('[data-test="timeline-action-move"]').length,
   build: globalThis.__PLANNER_BUILD_ID__, url: location.origin })
```

---

## Queue, roughly in order of value

1. **Device paint gate** (above). Blocks closing the original report.
2. **`motion.spec.js:503`** — either implement the repair (cancel a stalled
   `nbnotchbodyin` when the stage opens) or delete a test that has never passed.
   Leaving a permanently-red assertion trains everyone to ignore red.
3. **`interaction-feedback.spec.js:41`** — drive focus with a real `Tab` instead
   of `.focus()`, or assert `:focus-visible` explicitly.
4. **`toBeCloseTo(sample.progress, 0.08)`** in `navigation-shell.spec.js` — the
   second argument is *numDigits*, not tolerance. Effective tolerance is **±0.416**,
   ~5× looser than it reads; at mid-travel it admits `scaleX` anywhere in roughly
   [0.08, 0.92]. The `< 1.5px` width/height assertions carry the real weight.
5. **`useNavigationMotion.js` rAF loop** — travel is "browser-owned" for geometry,
   but a `requestAnimationFrame` clock still runs every frame writing
   `shell.dataset.navProgress`, whose only consumer is the test suite.
   `data-nav-progress` appears in no CSS selector so it is cheap, but there are
   **two writers** — React from state (`navigation.jsx`) and the imperative loop —
   and an unrelated re-render mid-travel clobbers the live value with a stale one.
   Three specs poll that attribute. Latent flake.
6. **`RIBBON_FALLBACK_CELL_WIDTH = 80`** (`ribbonViewport.js:20`) with real cells
   at 64/80/96 (`w-16 sm:w-20 lg:w-24`) — correct at exactly one breakpoint. Used
   unguarded in `useRibbonViewport.js:323` and `:342`. Measured drift at 1280:
   correct window start 342 vs 414 by fallback — **72 days against a 56-day render
   window**. Forcing one zero-width scroll frame reproduces a fully blank ribbon
   that does not self-correct until a date change. PR #10 fixed a *different*
   readiness bug and left this path untouched; pre- and post-#10 behave
   identically here. The guard already exists next door: `reveal()` treats a
   zero-width strip as `blocked-zero-width` and refuses to act. The scroll path
   should do the same instead of substituting a constant.
7. **Ribbon roving tab stop trade** (introduced in PR #13): if the selected day
   scrolls outside the rendered window, the ribbon has **zero** tab stops until you
   return to it. Reachable via `←`/`→`, `T` and the palette, so judged acceptable —
   but a fallback stop is a reasonable thing to want.
8. **Ten smart views, six chips.** README promises Today, Inbox, Upcoming,
   Deadlines, Overdue, Waiting, Someday, Unscheduled, Completed, All. The row
   renders six. Either the doc or the surface is wrong.
9. **`.planning/` is stale enough to mislead.** Written 2026-08-13; says
   `Planner.jsx` is 8,318 / 7,981 lines (it is 5,545) and lists provider sync as
   out of scope while `PRODUCT.md` commits to it. It is also in `.gitignore` while
   25 of its files are tracked, so it looks ignored and is not.
   `docs/spec/structure.md` still says "the 8k-line Planner".

---

## Environment — this remote box specifically

Everything in the 19 Aug handoff still applies. These are additions.

- **Playwright cannot find its own browser.** This image ships Chromium 1194;
  Playwright 1.62 wants 1234 and cannot download. Every run needs
  `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
  Without it you get ~58 identical "Executable doesn't exist" failures that look
  like a catastrophic regression and are not.
- **Use a distinct `PLAYWRIGHT_PORT` per run.** Ports were burned freely this
  session (4321, 4460, 4500, 4560…). A stale preview on a reused port serves the
  wrong bundle silently.
- **A scratchpad script cannot resolve `@playwright/test`.** Write probes into the
  repo root, run them, delete them. This is in the 19 Aug handoff and I hit it
  anyway.
- **`selectors.setTestIdAttribute("data-test")`** outside the Playwright config,
  or `getByTestId` matches nothing and reports it as a missing element.
- **Load contamination is real and it lied to me twice.** An `editor-rows` failure
  and a batch of `view-pills`/`typography` failures both evaporated on a quiet
  re-run. Check the wall clock of the run before believing any new failure — it is
  the cheapest lie-detector this suite has.
- Git worktrees under `.worktrees/` are gitignored and work well for bisecting;
  symlink `node_modules` in rather than re-installing.

---

## Method notes — what earned its keep, and what I got wrong

**What worked:**

- **Reading pixels beats reading screenshots.** I twice misjudged a rendered
  screenshot by eye — once calling a correct recessed card "wrong" because the
  shell (`rgb(23,24,27)`) and the surface (`rgb(10,10,12)`) are visually identical
  at a glance. Sampling the composited pixels through a canvas in a second page
  settled both questions in seconds and would have saved the wrong call.
- **Proving a cause by removing it.** Hiding only the four corner masks made
  PR #11's corner pixel-match `main`. That converted a hypothesis into a fact and
  named the fix at the same time.
- **Bisecting into a worktree.** Establishing that `motion.spec.js:503/727` fail
  at the exact commits that introduced them — that they had *never* passed —
  changed what those failures meant entirely.
- **Negative controls, every time.** All six new assertions in PR #13 were watched
  failing against the unfixed tree before being trusted (6 failed / 14 passed).

**What I got wrong, so the next session doesn't repeat it:**

- Grabbed an event card 14px from its top, landed in the desktop resize overlay,
  and reported "body drag resizes instead of moving". It was my grab point.
- Diffed task arrays index-wise and missed that dragging a recurring occurrence
  **appends** a detached task, concluding "action drag is broken" when it works.
- Predicted the eight transform walls would seam at fractional DPR, tested it at
  1/1.25/1.5/2, and found **no seam**. Worth recording as a negative: the
  geometry overlaps or abuts exactly, and the QA doc's reasoning that DPR checks
  matter less for this design is backwards even though its conclusion holds.
- **Broke focus-into-drawer with my own `inert` fix** and only caught it because I
  went looking for downstream impact. Making the drawer genuinely inert meant the
  focus call — issued in the same tick as `setPhase` — landed on an inert element
  and was dropped silently. One `requestAnimationFrame` was still too early; it
  needs two, which is what `useRibbonViewport` already waits for. **Nothing in the
  suite covered that behaviour.** It does now.

The lesson under all four: a green suite and a plausible screenshot are both
weaker evidence than a measurement of the thing itself.
