---
phase: 02-incremental-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domains/notes/model/note.js
  - src/domains/notes/queries/noteQueries.js
  - src/domains/notes/commands/noteCommands.js
  - src/domains/notes/index.js
  - src/domains/notes/tests/notes.test.js
autonomous: true
requirements:
  - NOTE-02
  - NOTE-03

must_haves:
  truths:
    - "Each user/date has at most one primary daily note"
    - "Opening the default daily-note editor resolves the primary note; if none exists, the first saved note becomes primary"
    - "A date may have additional day-linked notes created through an explicit add action"
    - "Users may designate another day-linked note as primary through an explicit action"
    - "Changing timezone does not silently move date-only daily notes"
  artifacts:
    - "src/domains/notes/queries/noteQueries.js contains getPrimaryDailyNote and updated getNotesForDate"
    - "src/domains/notes/commands/noteCommands.js contains createAdditionalDailyNote and designatePrimaryDailyNote"
    - "src/domains/notes/tests/notes.test.js verifies primary daily note uniqueness and explicit additional notes"
  key_links:
    - "src/domains/notes/index.js re-exports getPrimaryDailyNote, createAdditionalDailyNote, and designatePrimaryDailyNote"
---

<objective>
Implement pure domain operations and invariant enforcement in the Notes domain for primary daily notes and explicit additional day-linked notes.

Purpose: Reconcile daily-note cardinality so that the daily workflow edits the single primary daily note by default, while allowing users to attach additional notes to a day and designate primary notes explicitly.
Output: Domain model, queries, commands, and unit tests in `src/domains/notes/`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/product/planner-foundation.md
@docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
@src/domains/notes/index.js
@src/domains/notes/model/note.js
@src/domains/notes/queries/noteQueries.js
@src/domains/notes/commands/noteCommands.js
@src/domains/notes/tests/notes.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain Queries and Invariants for Primary Daily Notes (NOTE-02)</name>
  <files>src/domains/notes/model/note.js, src/domains/notes/queries/noteQueries.js, src/domains/notes/index.js</files>
  <read_first>
    src/domains/notes/model/note.js
    src/domains/notes/queries/noteQueries.js
    src/domains/notes/index.js
    docs/product/planner-foundation.md
    docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
  </read_first>
  <action>
    1. In `src/domains/notes/model/note.js`, ensure `normalizeNote` supports distinguishing primary daily notes from additional day-linked notes (e.g. `isPrimary` property or primary designation metadata on `kind: "daily"` notes with default `isPrimary: false` or `isPrimary: true` for standalone daily notes), keeping schema v8 compatibility.
    2. In `src/domains/notes/queries/noteQueries.js`:
       - Export `getPrimaryDailyNote(notes, dateKey)`: asserts `dateKey`, finds the active note with `note.kind === "daily" && note.date === dateKey && note.isPrimary !== false` (or explicitly designated primary).
       - Maintain `getDailyNote(notes, dateKey)` as an alias / delegation to `getPrimaryDailyNote(notes, dateKey)`.
       - Update `getNotesForDate(notes, dateKey)`: returns all active notes for `dateKey` (both primary daily note and additional day-linked notes) sorted with primary note first, then by `updatedAt`.
    3. In `src/domains/notes/index.js`, export `getPrimaryDailyNote`.
  </action>
  <verify>node --test src/domains/notes/tests/notes.test.js</verify>
  <acceptance_criteria>
    - `src/domains/notes/queries/noteQueries.js` exports `getPrimaryDailyNote`
    - `getDailyNote` and `getPrimaryDailyNote` return the primary daily note for a date
    - `getNotesForDate` returns both primary and additional day-linked notes for a date
    - `src/domains/notes/index.js` exports `getPrimaryDailyNote`
  </acceptance_criteria>
  <done>Domain queries resolve primary daily note and group day-linked notes deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Domain Commands for Explicit Additional Notes and Designating Primary (NOTE-03)</name>
  <files>src/domains/notes/commands/noteCommands.js, src/domains/notes/index.js, src/domains/notes/tests/notes.test.js</files>
  <read_first>
    src/domains/notes/commands/noteCommands.js
    src/domains/notes/queries/noteQueries.js
    src/domains/notes/tests/notes.test.js
    docs/product/planner-foundation.md
    docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
  </read_first>
  <action>
    1. In `src/domains/notes/commands/noteCommands.js`:
       - Export `createAdditionalDailyNote(notes, input, { now = null } = {})`:
         Asserts valid `dateKey`. Creates a new day-linked note with `kind: "daily"`, `date: dateKey`, `isPrimary: false`. Emits `NoteCreated` event.
       - Export `designatePrimaryDailyNote(notes, noteId, dateKey, { now = null } = {})`:
         Finds note with `id === noteId` and `date === dateKey`. If not found, throws `NoteValidationError`.
         Demotes any existing primary note for `dateKey` (`isPrimary: false`) and sets target note `isPrimary: true`.
         Emits `PrimaryDailyNoteDesignated` and `NoteChanged` events.
       - In `createNote`, if creating a `kind: "daily"` note where no primary daily note exists for that `date`, mark it `isPrimary: true`. If a primary daily note already exists and `isPrimary` is not specified as false, reject or enforce uniqueness invariant.
    2. In `src/domains/notes/index.js`, export `createAdditionalDailyNote` and `designatePrimaryDailyNote`.
    3. In `src/domains/notes/tests/notes.test.js`, add unit tests verifying:
       - Creating first daily note makes it primary.
       - Creating additional daily note via `createAdditionalDailyNote` succeeds without conflicting with primary note.
       - `designatePrimaryDailyNote` switches the primary note for a day and demotes the previous primary note.
       - Querying `getPrimaryDailyNote` returns the new primary note.
       - Querying `getNotesForDate` returns both notes.
  </action>
  <verify>node --test src/domains/notes/tests/notes.test.js</verify>
  <acceptance_criteria>
    - `src/domains/notes/commands/noteCommands.js` exports `createAdditionalDailyNote` and `designatePrimaryDailyNote`
    - `designatePrimaryDailyNote` demotes prior primary and designates target note
    - `src/domains/notes/tests/notes.test.js` passes all tests verifying NOTE-02 and NOTE-03
  </acceptance_criteria>
  <done>Domain commands for additional daily notes and designate-primary are implemented, exported, and verified by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test src/domains/notes/tests/notes.test.js` passes
- [ ] `npm test` passes full regression suite
- [ ] Export surface in `src/domains/notes/index.js` matches specifications
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- NOTE-02 and NOTE-03 requirements fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/02-incremental-hardening/02-01-SUMMARY.md`
</output>
