---
phase: 02-incremental-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/features/notes/dailyNoteResolution.js
  - src/features/notes/dailyNoteResolution.test.js
  - src/domains/planner/queries/dayAggregate.js
  - src/Planner.jsx
autonomous: true
requirements:
  - ARCH-03
  - PROD-03

must_haves:
  truths:
    - "New behavior extracts beside the owner in src/features/notes/ and src/domains/ without growing Planner.jsx"
    - "The daily note editor resolves the primary daily note for a date or establishes a new primary note on initial save"
    - "Day aggregate projections include primary daily note and all active day-linked notes"
    - "External calendar and task provider sync remain deferred"
  artifacts:
    - "src/features/notes/dailyNoteResolution.js encapsulates daily note resolution and draft handling"
    - "src/features/notes/dailyNoteResolution.test.js tests resolution edge cases"
    - "src/domains/planner/queries/dayAggregate.js projects primary daily note and notes list cleanly"
  key_links:
    - "src/Planner.jsx calls getDayAggregate and daily note resolution helper rather than holding inline domain logic"
---

<objective>
Extract daily note resolution and day projection handling beside the owner in `src/features/notes/` and `src/domains/planner/`, maintaining `Planner.jsx` as a stable composition root without adding domain rules to it.

Purpose: Enforce ARCH-03 (extract beside owner, do not grow `Planner.jsx`), BD-02/BD-11, and PROD-03 (provider sync deferred) while integrating the reconciled primary/additional daily notes model into application features.
Output: Feature helpers, projection queries, and tests colocated in `src/features/notes/` and `src/domains/planner/`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/spec/structure.md
@docs/adr/0001-domain-oriented-modular-monolith.md
@docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
@src/domains/notes/index.js
@src/domains/planner/queries/dayAggregate.js
@src/features/notes/contextLink.js
@src/Planner.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Daily Note Resolution Feature Beside Owner (ARCH-03, PROD-03)</name>
  <files>src/features/notes/dailyNoteResolution.js, src/features/notes/dailyNoteResolution.test.js</files>
  <read_first>
    src/features/notes/contextLink.js
    src/domains/notes/index.js
    docs/spec/structure.md
    docs/adr/0001-domain-oriented-modular-monolith.md
    docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
  </read_first>
  <action>
    1. Create `src/features/notes/dailyNoteResolution.js`:
       - Export `resolveDailyNoteDraft(notes, dateKey)`: resolves the primary daily note for `dateKey` if it exists; otherwise returns a clean draft template with `kind: "daily"`, `date: dateKey`, and empty blocks without mutating stored state.
       - Export `saveDailyNoteDraft(notes, draft, { now = null } = {})`: if `draft.id` exists, updates the note via `updateNote`; if not, creates a new primary daily note via `createNote` or `createAdditionalDailyNote` if `draft.isPrimary === false`.
    2. Create `src/features/notes/dailyNoteResolution.test.js`:
       - Unit test resolving an existing primary note.
       - Unit test resolving a draft for an unnoted date (returns non-persisted draft, leaves notes collection unchanged).
       - Unit test saving draft creates primary daily note with correct events.
  </action>
  <verify>node --test src/features/notes/dailyNoteResolution.test.js</verify>
  <acceptance_criteria>
    - `src/features/notes/dailyNoteResolution.js` exists and exports `resolveDailyNoteDraft` and `saveDailyNoteDraft`
    - `src/features/notes/dailyNoteResolution.test.js` passes all tests
    - No provider APIs or sync logic are imported or stubbed (PROD-03)
  </acceptance_criteria>
  <done>Daily note resolution feature extracted beside owner with dedicated unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Day Aggregate and Planner Integration without Growing Planner.jsx (ARCH-03)</name>
  <files>src/domains/planner/queries/dayAggregate.js, src/Planner.jsx</files>
  <read_first>
    src/domains/planner/queries/dayAggregate.js
    src/features/notes/dailyNoteResolution.js
    src/Planner.jsx
    docs/spec/structure.md
    docs/plans/2026-08-13-calendar-master-implementation-master-plan.md
  </read_first>
  <action>
    1. In `src/domains/planner/queries/dayAggregate.js`:
       - Ensure `getDayAggregate` uses `getPrimaryDailyNote(state.notes, selectedDate)` for `dailyNote` and `getNotesForDate(state.notes, selectedDate)` for `notes`.
    2. In `src/Planner.jsx`:
       - Use `resolveDailyNoteDraft` / `getPrimaryDailyNote` where daily note editing or target resolution occurs.
       - Do NOT append inline logic, helpers, or state machines to `Planner.jsx`.
       - Keep line count and complexity in `Planner.jsx` bounded or reduced.
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/domains/planner/queries/dayAggregate.js` accurately reflects primary note and day-linked notes
    - `src/Planner.jsx` delegates to extracted helpers and does not grow in LOC
    - Full test suite passes via `npm test`
  </acceptance_criteria>
  <done>Day aggregate and Planner integration clean and verified with full regression pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test src/features/notes/dailyNoteResolution.test.js` passes
- [ ] `npm test` passes all tests across the repository
- [ ] `git diff --stat src/Planner.jsx` verifies `Planner.jsx` has not grown
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- ARCH-03 and PROD-03 requirements fulfilled
- Modular monolith and living contract boundaries respected
</success_criteria>

<output>
After completion, create `.planning/phases/02-incremental-hardening/02-02-SUMMARY.md`
</output>
